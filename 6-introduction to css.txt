1) what is css 
	-cascading style sheet
	-style sheet is a type of language 
2) how to add css
	-inline, internal, external 
	-inline : same line as an element
	-css breakdow = background : property 
	-internal <style> html {css code} < /style >
	-selector (to specifie the element that we need)
	-external 

3) selectors
	-they re used to choose the part of the html code that will be modified
	-element selector : element{} 
	-classs selector : .class {}
	-<tag class = "class name> </tag>
	-# id selector
	-<tag id = "class name> </tag>
	-id is for 1 element, class can be used for differernt element
	-attribute selector : element[attribute]{css code}
	-universal selector : *{css code}
